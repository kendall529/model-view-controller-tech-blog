<section id="homepage">
    <div class="post-list-container">

        {{#each posts as |post|}}
        
        <div class="post-list-cards">

            <h2><a href="/posts/{{post.id}}">{{post.title}}</a></h2>

        </div>

        {{/each}}
    </div>
</section>